'use strict';
var $ = require('core-js-pure/internals/export');
var anObject = require('core-js-pure/internals/an-object');
var objectGetPrototypeOf = require('core-js-pure/internals/object-get-prototype-of');
var CORRECT_PROTOTYPE_GETTER = require('core-js-pure/internals/correct-prototype-getter');

// `Reflect.getPrototypeOf` method
// https://tc39.es/ecma262/#sec-reflect.getprototypeof
$({ target: 'Reflect', stat: true, sham: !CORRECT_PROTOTYPE_GETTER }, {
  getPrototypeOf: function getPrototypeOf(target) {
    return objectGetPrototypeOf(anObject(target));
  }
});
