'use strict';
var $ = require('core-js-pure/internals/export');
var $trim = require('core-js-pure/internals/string-trim').trim;
var forcedStringTrimMethod = require('core-js-pure/internals/string-trim-forced');

// `String.prototype.trim` method
// https://tc39.es/ecma262/#sec-string.prototype.trim
$({ target: 'String', proto: true, forced: forcedStringTrimMethod('trim') }, {
  trim: function trim() {
    return $trim(this);
  }
});
