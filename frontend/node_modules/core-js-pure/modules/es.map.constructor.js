'use strict';
var collection = require('core-js-pure/internals/collection');
var collectionStrong = require('core-js-pure/internals/collection-strong');

// `Map` constructor
// https://tc39.es/ecma262/#sec-map-objects
collection('Map', function (init) {
  return function Map() { return init(this, arguments.length ? arguments[0] : undefined); };
}, collectionStrong);
