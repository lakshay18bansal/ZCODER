'use strict';
var isCallable = require('core-js-pure/internals/is-callable');
var isObject = require('core-js-pure/internals/is-object');
var definePropertyModule = require('core-js-pure/internals/object-define-property');
var isPrototypeOf = require('core-js-pure/internals/object-is-prototype-of');
var wellKnownSymbol = require('core-js-pure/internals/well-known-symbol');
var makeBuiltIn = require('core-js-pure/internals/make-built-in');

var HAS_INSTANCE = wellKnownSymbol('hasInstance');
var FunctionPrototype = Function.prototype;

// `Function.prototype[@@hasInstance]` method
// https://tc39.es/ecma262/#sec-function.prototype-@@hasinstance
if (!(HAS_INSTANCE in FunctionPrototype)) {
  definePropertyModule.f(FunctionPrototype, HAS_INSTANCE, { value: makeBuiltIn(function (O) {
    if (!isCallable(this) || !isObject(O)) return false;
    var P = this.prototype;
    return isObject(P) ? isPrototypeOf(P, O) : O instanceof this;
  }, HAS_INSTANCE) });
}
