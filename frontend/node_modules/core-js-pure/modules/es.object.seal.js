'use strict';
var $ = require('core-js-pure/internals/export');
var isObject = require('core-js-pure/internals/is-object');
var onFreeze = require('core-js-pure/internals/internal-metadata').onFreeze;
var FREEZING = require('core-js-pure/internals/freezing');
var fails = require('core-js-pure/internals/fails');

// eslint-disable-next-line es/no-object-seal -- safe
var $seal = Object.seal;
var FAILS_ON_PRIMITIVES = fails(function () { $seal(1); });

// `Object.seal` method
// https://tc39.es/ecma262/#sec-object.seal
$({ target: 'Object', stat: true, forced: FAILS_ON_PRIMITIVES, sham: !FREEZING }, {
  seal: function seal(it) {
    return $seal && isObject(it) ? $seal(onFreeze(it)) : it;
  }
});
