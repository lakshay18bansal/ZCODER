'use strict';
var $ = require('core-js-pure/internals/export');
var fails = require('core-js-pure/internals/fails');
var toObject = require('core-js-pure/internals/to-object');
var nativeGetPrototypeOf = require('core-js-pure/internals/object-get-prototype-of');
var CORRECT_PROTOTYPE_GETTER = require('core-js-pure/internals/correct-prototype-getter');

var FAILS_ON_PRIMITIVES = fails(function () { nativeGetPrototypeOf(1); });

// `Object.getPrototypeOf` method
// https://tc39.es/ecma262/#sec-object.getprototypeof
$({ target: 'Object', stat: true, forced: FAILS_ON_PRIMITIVES, sham: !CORRECT_PROTOTYPE_GETTER }, {
  getPrototypeOf: function getPrototypeOf(it) {
    return nativeGetPrototypeOf(toObject(it));
  }
});

