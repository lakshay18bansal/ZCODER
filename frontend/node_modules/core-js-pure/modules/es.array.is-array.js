'use strict';
var $ = require('core-js-pure/internals/export');
var isArray = require('core-js-pure/internals/is-array');

// `Array.isArray` method
// https://tc39.es/ecma262/#sec-array.isarray
$({ target: 'Array', stat: true }, {
  isArray: isArray
});
