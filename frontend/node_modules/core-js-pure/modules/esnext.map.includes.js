'use strict';
var $ = require('core-js-pure/internals/export');
var sameValueZero = require('core-js-pure/internals/same-value-zero');
var aMap = require('core-js-pure/internals/a-map');
var iterate = require('core-js-pure/internals/map-iterate');

// `Map.prototype.includes` method
// https://github.com/tc39/proposal-collection-methods
$({ target: 'Map', proto: true, real: true, forced: true }, {
  includes: function includes(searchElement) {
    return iterate(aMap(this), function (value) {
      if (sameValueZero(value, searchElement)) return true;
    }, true) === true;
  }
});
