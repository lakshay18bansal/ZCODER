'use strict';
var $ = require('core-js-pure/internals/export');
var trunc = require('core-js-pure/internals/math-trunc');

// `Math.trunc` method
// https://tc39.es/ecma262/#sec-math.trunc
$({ target: 'Math', stat: true }, {
  trunc: trunc
});
