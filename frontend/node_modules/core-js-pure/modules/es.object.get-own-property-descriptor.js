'use strict';
var $ = require('core-js-pure/internals/export');
var fails = require('core-js-pure/internals/fails');
var toIndexedObject = require('core-js-pure/internals/to-indexed-object');
var nativeGetOwnPropertyDescriptor = require('core-js-pure/internals/object-get-own-property-descriptor').f;
var DESCRIPTORS = require('core-js-pure/internals/descriptors');

var FORCED = !DESCRIPTORS || fails(function () { nativeGetOwnPropertyDescriptor(1); });

// `Object.getOwnPropertyDescriptor` method
// https://tc39.es/ecma262/#sec-object.getownpropertydescriptor
$({ target: 'Object', stat: true, forced: FORCED, sham: !DESCRIPTORS }, {
  getOwnPropertyDescriptor: function getOwnPropertyDescriptor(it, key) {
    return nativeGetOwnPropertyDescriptor(toIndexedObject(it), key);
  }
});
