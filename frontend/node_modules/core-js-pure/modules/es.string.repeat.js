'use strict';
var $ = require('core-js-pure/internals/export');
var repeat = require('core-js-pure/internals/string-repeat');

// `String.prototype.repeat` method
// https://tc39.es/ecma262/#sec-string.prototype.repeat
$({ target: 'String', proto: true }, {
  repeat: repeat
});
