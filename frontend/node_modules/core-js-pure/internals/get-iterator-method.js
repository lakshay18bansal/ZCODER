'use strict';
var classof = require('core-js-pure/internals/classof');
var getMethod = require('core-js-pure/internals/get-method');
var isNullOrUndefined = require('core-js-pure/internals/is-null-or-undefined');
var Iterators = require('core-js-pure/internals/iterators');
var wellKnownSymbol = require('core-js-pure/internals/well-known-symbol');

var ITERATOR = wellKnownSymbol('iterator');

module.exports = function (it) {
  if (!isNullOrUndefined(it)) return getMethod(it, ITERATOR)
    || getMethod(it, '@@iterator')
    || Iterators[classof(it)];
};
