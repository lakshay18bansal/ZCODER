'use strict';
var getBuiltIn = require('core-js-pure/internals/get-built-in');
var uncurryThis = require('core-js-pure/internals/function-uncurry-this');
var getOwnPropertyNamesModule = require('core-js-pure/internals/object-get-own-property-names');
var getOwnPropertySymbolsModule = require('core-js-pure/internals/object-get-own-property-symbols');
var anObject = require('core-js-pure/internals/an-object');

var concat = uncurryThis([].concat);

// all object keys, includes non-enumerable and symbols
module.exports = getBuiltIn('Reflect', 'ownKeys') || function ownKeys(it) {
  var keys = getOwnPropertyNamesModule.f(anObject(it));
  var getOwnPropertySymbols = getOwnPropertySymbolsModule.f;
  return getOwnPropertySymbols ? concat(keys, getOwnPropertySymbols(it)) : keys;
};
