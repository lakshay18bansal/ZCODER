'use strict';
var shared = require('core-js-pure/internals/shared');
var uid = require('core-js-pure/internals/uid');

var keys = shared('keys');

module.exports = function (key) {
  return keys[key] || (keys[key] = uid(key));
};
