'use strict';
var path = require('core-js-pure/internals/path');
var hasOwn = require('core-js-pure/internals/has-own-property');
var wrappedWellKnownSymbolModule = require('core-js-pure/internals/well-known-symbol-wrapped');
var defineProperty = require('core-js-pure/internals/object-define-property').f;

module.exports = function (NAME) {
  var Symbol = path.Symbol || (path.Symbol = {});
  if (!hasOwn(Symbol, NAME)) defineProperty(Symbol, NAME, {
    value: wrappedWellKnownSymbolModule.f(NAME)
  });
};
