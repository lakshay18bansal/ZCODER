'use strict';
var DESCRIPTORS = require('core-js-pure/internals/descriptors');
var definePropertyModule = require('core-js-pure/internals/object-define-property');
var createPropertyDescriptor = require('core-js-pure/internals/create-property-descriptor');

module.exports = function (object, key, value) {
  if (DESCRIPTORS) definePropertyModule.f(object, key, createPropertyDescriptor(0, value));
  else object[key] = value;
};
