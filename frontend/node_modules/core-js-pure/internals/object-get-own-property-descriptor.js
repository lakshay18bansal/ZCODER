'use strict';
var DESCRIPTORS = require('core-js-pure/internals/descriptors');
var call = require('core-js-pure/internals/function-call');
var propertyIsEnumerableModule = require('core-js-pure/internals/object-property-is-enumerable');
var createPropertyDescriptor = require('core-js-pure/internals/create-property-descriptor');
var toIndexedObject = require('core-js-pure/internals/to-indexed-object');
var toPropertyKey = require('core-js-pure/internals/to-property-key');
var hasOwn = require('core-js-pure/internals/has-own-property');
var IE8_DOM_DEFINE = require('core-js-pure/internals/ie8-dom-define');

// eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
var $getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;

// `Object.getOwnPropertyDescriptor` method
// https://tc39.es/ecma262/#sec-object.getownpropertydescriptor
exports.f = DESCRIPTORS ? $getOwnPropertyDescriptor : function getOwnPropertyDescriptor(O, P) {
  O = toIndexedObject(O);
  P = toPropertyKey(P);
  if (IE8_DOM_DEFINE) try {
    return $getOwnPropertyDescriptor(O, P);
  } catch (error) { /* empty */ }
  if (hasOwn(O, P)) return createPropertyDescriptor(!call(propertyIsEnumerableModule.f, O, P), O[P]);
};
