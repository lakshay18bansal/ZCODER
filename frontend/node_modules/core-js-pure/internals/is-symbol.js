'use strict';
var getBuiltIn = require('core-js-pure/internals/get-built-in');
var isCallable = require('core-js-pure/internals/is-callable');
var isPrototypeOf = require('core-js-pure/internals/object-is-prototype-of');
var USE_SYMBOL_AS_UID = require('core-js-pure/internals/use-symbol-as-uid');

var $Object = Object;

module.exports = USE_SYMBOL_AS_UID ? function (it) {
  return typeof it == 'symbol';
} : function (it) {
  var $Symbol = getBuiltIn('Symbol');
  return isCallable($Symbol) && isPrototypeOf($Symbol.prototype, $Object(it));
};
